declare module "mount-vue-component"

/*
{
  import { App, ComponentOptions, VNode } from "vue"
  export function mount(
    component: any,
    options: VueMountOptions
  ): { vNode: VNode; destroy: () => {}; el: HTMLElement }
  export interface VueMountOptions {
    props?: any
    children?: any
    element?: HTMLElement
    app?: App<Element>
  }
}
*/
